[package]
name = "lau-worldgen"
version = "0.1.0"
edition = "2021"
description = "Procedural voxel world generator for the Lau platform"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
